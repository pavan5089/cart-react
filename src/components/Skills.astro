---
interface SkillCategory {
  title: string;
  icon: string;
  skills: string[];
}

const skillCategories: SkillCategory[] = [
  {
    title: "Frontend Development",
    icon: "🎨",
    skills: ["HTML5", "CSS3", "JavaScript", "React"]
  },
  {
    title: "Backend Development",
    icon: "⚙️",
    skills: ["Node.js", "Python", "Java", "SQL"]
  },
  {
    title: "DevOps & Tools",
    icon: "🛠",
    skills: ["Git", "Docker", "AWS", "CI/CD"]
  },
  {
    title: "Soft Skills",
    icon: "🤝",
    skills: ["Team Leadership", "Communication", "Problem Solving", "Mentoring"]
  }
];
---
<section id="skills" class="py-20 bg-gray-900 text-white">
  <div class="max-w-6xl mx-auto px-4">
    <h2 class="text-4xl font-bold text-center mb-16">Skills & Expertise</h2>
    <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
      {skillCategories.map((category, index) => (
        <div 
          class="bg-gray-800 p-6 rounded-lg transform transition-all duration-300 hover:scale-105 hover:shadow-xl hover:bg-gray-700 animate-fadeIn"
          style={`animation-delay: ${index * 200}ms`}
        >
          <div class="text-4xl mb-4 text-center transform transition-transform hover:scale-110 hover:rotate-6">
            {category.icon}
          </div>
          <h3 class="text-xl font-bold mb-4 text-center">{category.title}</h3>
          <ul class="space-y-2">
            {category.skills.map(skill => (
              <li class="flex items-center text-gray-300 transform transition-transform hover:translate-x-2">
                <span class="w-2 h-2 bg-blue-600 rounded-full mr-2"></span>
                {skill}
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </div>
</section>